import XCTest
@testable import LibraryCodingStyle

final class LibraryCodingStyleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LibraryCodingStyle().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
